---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: rightsizingpolicies.rightsizing.redhatconsulting.io
spec:
  group: rightsizing.redhatconsulting.io
  names:
    kind: RightsizingPolicy
    listKind: RightsizingPolicyList
    plural: rightsizingpolicies
    singular: rightsizingpolicy
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              algorithm:
                properties:
                  headroomPercent:
                    type: integer
                  percentile:
                    type: integer
                required:
                - headroomPercent
                - percentile
                type: object
              autoMode:
                properties:
                  enabled:
                    type: boolean
                  requireApproval:
                    type: boolean
                  schedule:
                    type: string
                required:
                - enabled
                - requireApproval
                - schedule
                type: object
              lookbackDays:
                type: integer
              reconcileIntervalMinutes:
                type: integer
              revertRetentionDays:
                type: integer
              thresholds:
                properties:
                  minCpuSavings:
                    format: int32
                    type: integer
                  minMemorySavings:
                    anyOf:
                    - type: integer
                    - type: string
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  upsizeUtilizationPercent:
                    type: integer
                required:
                - minCpuSavings
                - minMemorySavings
                - upsizeUtilizationPercent
                type: object
            required:
            - algorithm
            - autoMode
            - lookbackDays
            - reconcileIntervalMinutes
            - revertRetentionDays
            - thresholds
            type: object
        type: object
    served: true
    storage: true
